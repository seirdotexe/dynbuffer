import assert from 'node:assert/strict';
import { describe, it } from 'node:test';
import DynBuffer from '../index.js';

describe('byte test', () => {
  it('should support writing and reading a signed byte', () => {
    // Todo
  });

  it('should support writing and reading an unsigned byte', () => {
    // Todo
  });
});